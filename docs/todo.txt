
externalize:
    GftService (tanuki wrapper)
    util.Database (dbcp)
    odwek package (ODApi)

    mq package (MsgMover)
    admin QueueStatus
    GftCli (move message option, indirect via JmsSystem)
    JmsListener (setTargetClient in error message)
    JmsSystem createMqManager only used in gftCli.moveMessage and QueueStatus
    ?? jndi based config ??

    jgit GftCli

    ActiveMqsystem, GftCli,

release 4.0:
    - no odwek, tanuki, dbcp
    - ibm,mq for the time being
    - test resources needed
    - no GftCli? SpringBoot provides runnable jar
    - jetty replaced by Spring Boot?

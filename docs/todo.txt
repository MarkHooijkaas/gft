
externalize:
    mq package (MsgMover)

    JmsListener (setTargetClient in error message)
    JmsSystem createMqManager only used in gftCli.moveMessage and QueueStatus
    ?? jndi based config ??


    ??ActiveMqsystem

release 4.0:
    - no odwek, tanuki, dbcp, jgit
    - ibm.mq for the time being
    - test resources needed
    - jetty replaced by Spring Boot?

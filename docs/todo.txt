
externalize:
    GftService (tanuki wrapper)
    util.Database (dbcp)
    odwek package (ODApi)

    mq package (MsgMover)
    admin QueueStatus
    GftCli (move message option, indirect via JmsSystem)
    JmsListener (setTargetClient in error message)
    JmsSystem createMqManager only used in gftCli.moveMessage and QueueStatus
    ?? jndi based config ??

    jgit GftCli

    ActiveMqsystem, GftCli,